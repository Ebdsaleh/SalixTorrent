# Development-only dependency for Phase 12 locale generation.
# Normal SalixTorrent users and ordinary source/runtime installs do not need it.
# Authentication uses Google Application Default Credentials; no credentials are
# stored in SalixTorrent or in the generated locale packs.
google-cloud-translate>=3.27,<4
